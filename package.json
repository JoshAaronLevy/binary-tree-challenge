{
  "name": "binary-tree-challenge",
  "version": "1.0.0",
  "description": "Solve the coding challenge of the binary tree",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "nyc --reporter=html --reporter=text mocha --reporter mochawesome --recursive ./tests/*.spec.js"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "nyc": {
    "reporter": [
      "text",
      "lcov"
    ],
    "include": [
      "src/**/*.js"
    ],
    "exclude": [
      "test/**"
    ]
  },
  "dependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  },
  "devDependencies": {
    "mochawesome": "^7.1.3",
    "nyc": "^15.1.0"
  }
}
